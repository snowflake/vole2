{
    IBClasses = (
        {CLASS = FirstResponder; LANGUAGE = ObjC; SUPERCLASS = NSObject; }, 
        {
            ACTIONS = {
                doCancel = id; 
                doOK = id; 
                selectFillBackToSpecific = id; 
                selectFillExisting = id; 
                stopScan = id; 
            }; 
            CLASS = MissingMessagesController; 
            LANGUAGE = ObjC; 
            OUTLETS = {
                fillBackToSpecific = NSButtonCell; 
                fillExisting = NSButtonCell; 
                messageNumber = NSTextField; 
                missingMessagesProgressWindow = NSWindow; 
                missingMessagesWindow = NSWindow; 
                progressBar = NSProgressIndicator; 
                progressInfo = NSTextField; 
                stopButton = NSButton; 
            }; 
            SUPERCLASS = NSWindowController; 
        }
    ); 
    IBVersion = 1; 
}